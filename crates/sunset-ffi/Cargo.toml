[package]
name = "sunset-ffi"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "UniFFI binding layer between sunset-core and Kotlin/Swift UI."

[dependencies]
sunset-core = { path = "../sunset-core", version = "0.1.0" }

# UniFFI 将在核心 API 稳定后接入（TODO: 阶段 2）
# uniffi = { version = "0.29", features = ["cli"] }

[build-dependencies]
# uniffi = { version = "0.29", features = ["bindgen"] }

[lib]
crate-type = ["cdylib", "staticlib", "lib"]